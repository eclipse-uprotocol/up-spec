export default {
    featureDirectory: ".",
    customRulesDirectory: "./custom-rules",
    rules: {
        "allowed-tags": "off",
        "no-trailing-spaces": "error",
        "new-line-at-eof": "error",
    },
    fix: false,
}
